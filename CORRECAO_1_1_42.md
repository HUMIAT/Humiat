# Organiza 1.1.42

Correção emergencial na tela de Campanhas.

- Corrige `NameError: name 'Configuracao' is not defined`.
- A rotina `obter_pacote_atual()` passa a usar o model correto `ConfiguracaoSistema`.
- Nenhuma regra de campanha, fila, WhatsApp, promoção ou pacotes foi alterada.
